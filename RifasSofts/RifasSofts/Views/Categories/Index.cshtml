@model IEnumerable<RifasSofts.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CATEGORY_NAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CATEGORY_PIC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CATEGORY_NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CATEGORY_PIC)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CATEGORY_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CATEGORY_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CATEGORY_ID })
        </td>
    </tr>
}

</table>
